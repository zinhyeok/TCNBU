% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gSBS.R
\name{gSBS}
\alias{gSBS}
\title{Change-Point search by graph-based SBS}
\usage{
gSBS(
  y,
  s = 1,
  e = nrow(y),
  alpha = 0.01,
  decay.a = sqrt(0.5),
  minLen = 10,
  stat = "g",
  graph = "mst",
  kMax = 30,
  cutoff = 0.05,
  distType = "euclidean",
  maxLen = 1000,
  ...
)
}
\arguments{
\item{y}{A n x d matrix to be scanned with n observations and d dimensions.}

\item{s}{Position where the scan starts.}

\item{e}{Position where the scan ends.}

\item{alpha}{Significance level for each scan statistic.}

\item{decay.a}{Decay parameter used in SBS (between 0.5 and 1).}

\item{minLen}{Minimum length of generated intervals.}

\item{stat}{The scan statistic to be computed.

\if{html}{\out{<div class="sourceCode">}}\preformatted{ "g" specifies the generalized edge-count scan statistic;

 "o" specifies the original edge-count scan statistic;

 "w" specifies the weighted edge-count scan statistic;

 "m" specifies the max-type edge-count scan statistic.
}\if{html}{\out{</div>}}}

\item{graph}{The type of similarity graphs.

\if{html}{\out{<div class="sourceCode">}}\preformatted{"mst" specifies the minimum spanning tree;

"knn" specifies the nearest neighbor graph.
}\if{html}{\out{</div>}}}

\item{kMax}{Max k of the similarity graph (k-mst or knn).}

\item{cutoff}{Cutoff percentage at the beginning and end of each scan.}

\item{distType}{The distance measure to be used in the \code{\link[stats]{dist}} function.}

\item{maxLen}{Maximum length of generated intervals.}

\item{...}{Arguments to be passed to \code{\link[gSeg]{gseg1}}.}
}
\value{
Scanned candidate change-points.
}
\description{
This function find all potential change-points in a sequence by graph-based scan statistics and seeded binary segmentation.
}
\examples{
set.seed(1)
y <- rbind(
  matrix(rnorm(200), ncol = 10), matrix(rexp(200), ncol = 10),
  matrix(rnorm(200), ncol = 10), matrix(rexp(200), ncol = 10)
)
gSBS(y, s = 1, e = 80, alpha = 0.01, decay.a = sqrt(0.5), graph = "mst")
}
